<%= form_for(@enquiry) do |f| %>
    <% if @enquiry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enquiry.errors.count, "error") %> prohibited this pet from being saved:</h2>

      <ul>
      <% @enquiry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :reference %>
      <%= f.text_field :Reference %>
    </div>

    <div class="field">
      <%= f.label :Location %>
      <%= f.text_field :Location %>
    </div>


    <div class="field">
      <%= f.label :description %>
      <%= f.text_field :Description %>
    </div>

  <div class="field">
      <%= f.label :amount %>
      <br />
    <%= f.text_area :Amount, rows: 10 %>
    </div>

    <div class="field">>
      <%= f.label :date%>
      <%= f.text_field :Date %>
    </div>

    <div class="field">
      <%= f.label :time%>
      <%= f.text_field :Time %>
    </div>
<%# 24/11 MG vanaf hier zijn de extra tabellen toegevoegd. bovenstaande werkt!!! -%>

    <div class="field">
      <%= f.label :Done %>
      <%= enquiry_measures.text_field :done %>
    </div>

    <div class="field">
      <%= f.label :Needed %>
      <%= enquiry_measures.text_field :needed %>
    </div>


<%# 24/11 MG tot hier-%>

    <%= f.fields_for :enquiry_measures do |enquiry_measures| %>
        <%# enquiry_measures.fields_for :measure do |measures| %>
        <div>
          <%= f.label :Maatregel %>
          <br/>
          <%= collection_select(:measure_id, 0, Measure.all, :id, :measurement_type) %>

          <%= collection_select(:measure_id, 0, Measure.all, :id, :measurement, {:prompt => false}, {:multiple => true}) %>
        </div>

        <div>
          <%# test met enquiry_measures ipv f.label 7-12 MG%>
          <%= f.label :Gereed %>
          <br/>
          <%= enquiry_measures.check_box :done %>
        </div>

        <div>
          <%= f.label :Verantwoordelijke %>
          <br/>
          <%= enquiry_measures.text_field :responsible %>
        </div>

        <div>
          <%= f.label :Benodigd %>
          <br/>
          <%= enquiry_measures.check_box :needed %>

        </div>

    <%end%>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
