<h1>Editing Enquiry</h1>

<%= render 'form' %>

<h2>Tools</h2>
<% if @enquiry.tools.any? %>
    <ul>
      <% @enquiry.tools.each do |tool| %>
          <%# 3-01-16 MG link_to is nog niet getest!!! %>
      <li><%= link_to tool, tool %> </li>
      <%end%>
    </ul>
      <% else %>
    <p>No tools entered</p>
   <%end%>


<%= link_to 'Show', @enquiry %> |
<%= link_to 'Back', enquiries_path %>
